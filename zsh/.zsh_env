# Переменные окружения
export EDITOR=mousepad          # редактор по умолчанию
export VISUAL="$EDITOR"    # для GUI приложений
export PATH="$HOME/bin:$PATH"


# переменные текущей сессии (IP и т.п.)
[ -f ~/.session_vars ] && source ~/.session_vars
